name: place_picker
description: Plugin place picker.
version: 0.0.7
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^1.2.0
  http: ^0.13.4
  uuid: ^3.0.4
  location: ^4.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  # plugin:
